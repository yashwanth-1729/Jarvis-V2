{
  "name": "@jarvis-oss/sldt-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "The one stateless server-side component SLDT needs: forwards allowlisted write requests to GitHub's Contents API, holding the PAT so the browser never has to.",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "start": "npx tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "npx tsx tests/handler.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.23.12",
    "typescript": "^5.6.3"
  }
}

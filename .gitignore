# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# --- Secrets & local config ------------------------------------------------
.env
.env.local
!.env.example
!.env.local.example

# --- Database --------------------------------------------------------------
backend/storage/*.db
backend/storage/*.db-wal
backend/storage/*.db-shm
# Snapshot copies of the live database. The *.db rule above does not
# cover them, so a `git add -A` would otherwise commit personal data.
backend/storage/*.db.bak
backend/storage/*.db.pre-sync-*
backend/storage/*.db.*.bak
backend/storage/memory-vault/

# --- Node / Next -----------------------------------------------------------
node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo

# --- Editors / OS ----------------------------------------------------------
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local UI previews (isolated fixtures and device screenshots)
/tmp/mobile-review/

# Piper voice models — large ONNX binaries, fetched per machine (see explanations.md)
backend/models/piper/*.onnx
backend/models/piper/*.onnx.json
backend/.piptmp/

# Android Piper assets fetched by backend/tools/android/setup_piper.ps1 (large, not committed)
frontend/src-tauri/gen/android/app/libs/*.aar
frontend/src-tauri/gen/android/app/src/main/assets/piper/
# Machine-local pointer copied beside a personal Windows installation.
frontend/src-tauri/backend-path.local.txt
install-backups/
# Local scratch that can hold personal data: artifact staging (contained a copy
# of the real memory store) and raw device log captures (spoken transcripts).
.artifact_staging/
crash_capture*.log
verify_capture*.log
android_tts_capture*.log
backend/backend_live.log

# Built-in Google OAuth client for the app (kept out of the public repo; bundled into builds)
backend/app/connectors/builtin_google.json
